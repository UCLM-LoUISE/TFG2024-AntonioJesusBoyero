import { FormatearTipoTareaPipe } from './formatear-tipo-tarea.pipe';

describe('FormatearTipoTareaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearTipoTareaPipe();
    expect(pipe).toBeTruthy();
  });
});
